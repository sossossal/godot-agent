# 游戏开发标准化增强路线图

这份文档聚焦“如何让后续增强更稳定”，不是单纯罗列新功能。

目标有 4 个：

- 约束所有 AI 和工具输出，减少后期返工
- 约束文件树和产物落点，降低项目越做越乱的风险
- 按游戏类型沉淀通用模板，提高复用率
- 延续 `skills` 化思路，让不同 AI API 接入后仍保持质量、结构和测试一致

在这 4 个方向之外，还建议把以下底座一起纳入路线图：

- 统一契约层
- 命名与目录规范检查器
- 模板注册中心
- Skill 质量基线
- 可回放测试资产
- 预算系统
- 运营事件字典
- 迁移与兼容层

## 总体原则

### 1. 输出先有契约，再有实现

所有模块都应先定义结构化输入输出，再写逻辑。至少覆盖：

- `Task.context`
- `artifacts`
- `history`
- `release metadata`
- `data table schema`
- `telemetry events`
- `performance reports`

每个结构都应带：

- `schema_version`
- 必填字段
- 默认值
- 枚举范围
- 失败码或错误类别

### 2. 文件树先有规则，再允许生成

任何新资源、脚本、报告、导出产物都不应直接自由落盘。应先定义：

- 允许写入的目录白名单
- 文件命名规则
- 同类资源的固定落点
- 自动创建目录时的父级约束
- 重复命名和孤儿文件检查

### 3. 模板先注册，再允许实例化

模板不应散落在各 skill 内部匿名存在。需要一个统一注册中心，至少记录：

- 模板名称
- 模板版本
- 适用游戏类型
- 可覆盖参数
- 输出目录策略
- 依赖的资源和测试模板

### 4. Skill 先满足质量壳，再允许接 AI

不同 AI API 只负责“生成内容”，不能直接决定项目结构。每个 skill 都应自带统一壳层：

- 输入校验
- 目录校验
- 模板加载
- 生成后验证
- 产物摘要
- 回滚锚点
- 目标测试

## P0: 必须先补的标准化底座

### 1. 统一契约层

目标：

- 让内容生成、发布、遥测、性能、数据表都共享同一套结构化约束

建议新增：

- `agent_system/contracts/` 目录
- 统一的 schema 定义与校验入口
- `task / artifact / telemetry / performance / template manifest` 契约文件

完成标准：

- 新模块接入前必须先定义契约
- Portal、CLI、Router、MCP 输出字段一致
- 历史记录和发布产物可稳定反序列化

### 2. 文件树与命名规范引擎

目标：

- 把“约定目录”升级成“自动阻断错误落盘”

建议覆盖：

- 脚本目录
- 场景目录
- 资源目录
- 数据表目录
- 报告目录
- 发布目录
- 遥测和性能基线目录

建议规则：

- 禁止直接写入仓库根目录
- 禁止把测试基线混进运行目录
- 限制文件后缀与目录组合
- 检查重复资源、孤儿资源、错误命名

完成标准：

- 所有创建类 skill 在落盘前都会通过目录检查
- `doctor` 或独立命令可审计全项目目录健康度

### 3. 模板注册中心

目标：

- 把“按游戏类型建立通用模板”从文档建议变成真实机制

首批模板建议按类型分层：

- `platformer`
- `topdown_action`
- `arpg`
- `roguelike`
- `tower_defense`
- `visual_novel`
- `survival_crafting`

每个类型至少包含：

- 目录骨架
- 核心场景模板
- 基础脚本模板
- 默认数据表
- UI 模板
- 测试模板
- 性能预算模板

完成标准：

- 新项目初始化时可选择类型模板
- 模板升级有版本号和迁移说明

### 4. Skill 统一基线

目标：

- 让后续所有增强模块沿用相同交付模式

每个 skill 至少应输出：

- `summary`
- `artifacts`
- `validation`
- `rollback`
- `schema_version`
- `quality_gate`

每个 skill 至少应包含：

- 输入参数 schema
- 输出 schema
- 文件落点规则
- 模板依赖说明
- 最小回归测试

完成标准：

- 新增 skill 不再是“单个脚本功能”，而是完整的可验证模块

### 5. 预算系统与回放资产

目标：

- 给 QA、性能、画面差异建立统一基线

首批基线资产：

- golden screenshots
- 输入回放脚本
- 标准测试场景
- 标准数据表样本
- 性能基准结果

首批预算指标：

- `scene_load_ms`
- `fps_min`
- `memory_peak_mb`
- `draw_call_budget`
- `node_count_budget`
- `texture_budget_mb`

完成标准：

- 发布门禁不再只依赖单次运行结果，而有稳定对照基线

## P1: 面向五个增强方向的模块化建设

### 6. 美术内容生产链路

目标：

- 让美术资源进入与代码、数据表一样可审计的生产管线

建议 skills：

- `ingest_art_asset`
- `validate_ui_asset_pack`
- `build_texture_atlas_plan` 已落地为 `manage_art_asset_pipeline` 的 `atlas_plan`，会为 spritesheet/aseprite 生成 frame grid、atlas path、frame count 和预算状态
- `audit_material_resource_links` 已落地为 `manage_art_asset_pipeline` 的 `material_link_audit`，会识别 material/substance 的 albedo/normal/orm 等贴图通道、缺失项和链接数量
- `track_asset_provenance`

必须配套的约束：

- 分辨率、尺寸、格式约束
- 图集策略
- 命名规则
- 来源与版权记录
- 引用完整性检查

建议新增：

- 资源来源追踪和 license manifest 已纳入 `asset_review_workflow.provenance_summary`，会统计 source/target/source_tool/license/dependency 覆盖率与缺口资产

### 7. 关卡编辑器级工作流

目标：

- 把关卡制作从“零散节点注入”升级成“结构化关卡生产”

建议 skills：

- `create_level_template`
- `manage_spawn_points`
- `author_interaction_zones`
- `validate_navigation_and_collision`
- `run_playability_path_check`

必须配套的约束：

- 关卡 schema
- 刷怪点、交互点、存档点、触发区定义
- 关卡命名与目录分层
- 可玩路径验证

当前落地：

- `manage_level_workflow` 已统一到单一 skill contract，支持 `template / preview / audit / snapshot / diff`
- 关卡 manifest 已扩展 `navigation_agents / tile_layers / trigger_zones / level_bounds`
- 关卡场景模板会生成 `NavigationRegion2D/3D`、`NavigationAgent2D/3D`、`TileMap/GridMap`、`Area2D/3D` 和 `LevelBounds`
- `snapshot / diff` 会把结构化快照落到 `logs/test_artifacts/`，用于关卡结构回归
- API 已提供 `POST /levels/manage`，Portal/MCP/CLI 可直接复用同一套关卡工作流

### 8. 数值平衡分析

目标：

- 让数值表不只是可编辑，还能被分析和回归验证

建议 skills：

- `simulate_combat_balance` 已落地为 `analyze_game_balance` 的可选战斗仿真 gate，输出 TTK、承伤、存活边际、JSON 和 Markdown 报告
- `audit_growth_curve` 已落地为 `analyze_game_balance` 的可选成长曲线 gate，输出 enemy power / quest reward 的点、斜率、异常段、JSON 和 Markdown 报告
- `analyze_drop_distribution`
- `compare_balance_versions` 已落地为 `balance_version_compare` contract/report，支持 baseline-vs-candidate 的 score、issue、warning 和 metric drift 对比
- `detect_balance_outliers`

必须配套的约束：

- 数值表 schema
- 公式定义
- 采样输入
- 可回放仿真结果
- 版本对比报告

### 9. 运营数据与遥测回流

目标：

- 把试玩和运营反馈回流成结构化任务，不再靠口头描述

建议 skills：

- `define_event_catalog`
- `validate_telemetry_payloads`
- `ingest_playtest_feedback`
- `cluster_runtime_failures`
- `backfill_feature_feedback_tasks`

必须配套的约束：

- 事件命名规范
- 字段 schema
- 版本兼容策略
- Feature ID 关联
- 隐私与脱敏规则

建议新增：

- 遥测隐私边界和权限说明

当前落地：

- `manage_game_telemetry` 已升级到 `telemetry_summary 1.4`
- 结构化输出现在额外包含 `retention_cohorts / funnel_breakdown / retention_funnel_dashboard / crash_taxonomy / crash_clusters / crash_regression_dashboard / pii_violation_count / privacy_gate_passed`
- `agent_system/tools/telemetry_analysis.py` 已能按 `player_id / user_id / account_id / anonymous_id` 生成 `D1 / D3 / D7` 留存窗口
- `agent_system/skills/resource/export_skill.py` 的 telemetry gate 已把 `PII gate` 纳入 QA/release 质量门禁，并额外写出 `telemetry_d1_retention_rate / telemetry_d7_retention_rate / telemetry_largest_dropoff_* / telemetry_lowest_retention_*`
- `GET /portal/index.html` 的 `遥测回流` 面板已能直接展示 `Privacy Gate / Retention / Funnel Breakdown / Retention / Funnel Dashboard / Crash Taxonomy / Crash Clusters / Crash Regression`，并支持 `导出留存漏斗报告` 与 `导出 Crash 报告`
- API 已新增 `GET /telemetry/crash-clusters`，可直接返回 crash cluster 列表和 Markdown 排障摘要，供 Portal/MCP 复用
- API 已新增 `GET /telemetry/crash-dashboard`，可直接返回按 build/scene 聚合的 crash regression dashboard 和 Markdown 摘要，供 Portal/MCP 复用
- API 已新增 `GET /telemetry/retention-dashboard`，可直接返回 retention/funnel dashboard 和 Markdown 摘要，供 Portal/MCP 复用
- `tests/test_telemetry_pipeline.py`、`tests/test_api.py`、`tests/test_quality_dashboard.py` 与 `tests/test_agent.py` 已补齐对应回归覆盖
- 仓库内置样本 `telemetry/sessions/vertical_slice_sample.jsonl` 已升级为 5-session telemetry dataset，可稳定回归 `D1 / D3 / D7 retention`、partial funnel、`native` crash taxonomy 和 `stack_native_sigsegv_player_controller` crash cluster

### 10. 更重的性能分析体系

目标：

- 把当前性能门禁从“轻量发布检查”升级成“可定位瓶颈的体系”

建议 skills：

- `capture_performance_profile`
- `compare_screenshot_baselines`
- `audit_scene_complexity`
- `track_memory_regressions`
- `generate_performance_budget_report`

必须配套的约束：

- 统一性能采样格式
- 场景分级基线
- 平台预算模板
- 回归阈值
- 报告落点和历史归档

当前落地：

- `capture_performance_profile` 已落地为 `manage_game_performance` 的 `capture` action，会生成临时 Godot headless GDScript，采集场景加载耗时、节点数、基础内存指标、frame breakdown 和 memory trend，写入 `logs/test_artifacts/performance_profile_*.json`，再复用 `performance_summary 1.1` 与 Markdown 报告
- `manage_game_performance` 已提供 `baseline / validate / analyze` 三段式链路，输出 `performance_summary 1.1`、预算校验、基线回归、`frame_breakdown / memory_trend` 和 Markdown 报告
- `build_run_matrix` 已新增 `runtime_performance_sampling` lane，会自动读取 `logs/test_artifacts/performance_profile_*.json` 最新运行时画像，归一化为 `performance_summary 1.1`，并把 profile path、metric/check/issue/warning 数、top frame stage 和 memory trend 写入矩阵 row details
- `compare_screenshot_baselines` 已落地到 `GamePerformanceAnalyzer.compare_screenshot_baselines(...)` 与 `manage_game_performance`，可通过 `screenshot_baseline_path / screenshot_candidate_path` 计算 `screenshot_diff_ratio`，并复用 `max_screenshot_diff_ratio` 预算进入 `performance_summary`、API dashboard 和 Markdown 报告
- `track_memory_regressions` 已落地到 `GamePerformanceAnalyzer.track_memory_regressions(...)` 与 `performance_summary.memory_regression`，可对比 baseline/candidate 的 memory trend，输出 growth/peak/avg delta，并复用 `max_memory_growth_mb / max_memory_peak_delta_mb / max_memory_avg_delta_mb` 预算进入 API dashboard 和 Markdown 报告

## P2: 长期稳定性补强

### 11. 迁移与兼容层

目标：

- 模板、schema、目录升级后，旧项目仍可维护

建议建设：

- `migrations/` 目录
- 模板版本迁移脚本
- 数据表 schema 迁移脚本
- 性能基线更新策略

当前落地：

- `agent_system/migrations/` 已提供 P2 迁移兼容 runner
- `GET /migrations/status` 会检查模板 manifest、性能基线目录、遥测目录和数据表 schema
- `POST /migrations/apply` 只创建缺失受管目录，不自动重写用户数据
- `GET /genre-templates/marketplace` 会暴露模板来源、安装状态和校验结果，作为后续模板市场与版本迁移入口

### 12. 统一质量面板

目标：

- 在 Portal 中直接查看项目标准化状态

建议面板：

- 契约健康度
- 目录健康度
- 模板使用情况
- Skill 覆盖率
- 遥测健康度
- 性能预算达标率

当前落地：

- `agent_system/tools/quality_dashboard.py` 已统一汇总 contract、layout、template、skill、telemetry、performance 和 migrations
- `GET /quality/dashboard` 返回稳定 `schema_version=1.0` 的质量面板载荷
- Portal 检视器已新增“质量面板”，项目切换和任务更新后会自动刷新

### 13. 远程 MCP 与部署接入

目标：

- 让无法直接启动 stdio MCP 的 CLI、IDE 或私有网关也能接入同一套 Godot Agent 工具

当前落地：

- `bridge/remote_mcp_server.py` 提供 HTTP bridge
- 远程 bridge 复用 stdio MCP 的 `godot_make / godot_status / godot_capture` 工具 schema
- `GET /mcp/remote-manifest` 返回部署命令、manifest URL、tool call pattern 和安全提示
- Portal 的 `MCP / IDE` 面板已补充 Remote MCP Bridge 启动指引

## P3: 项目治理与变更准入

目标：

- 把“新功能必须带 schema、测试、模板、目录策略和迁移说明”从文档建议变成可执行准入门禁
- 让不同 AI API、CLI、IDE 或人工开发者提交变更时，都先声明同一套 evidence

首批治理对象：

- `feature`
- `skill`
- `template`
- `data_table`
- `telemetry`
- `performance`
- `release`
- `mcp_bridge`
- `portal`

当前落地：

- `agent_system/tools/governance.py` 已提供治理策略与准入检查器
- `GET /governance/policy` 会返回变更类型、必需 evidence、evidence catalog 和允许路径策略
- `POST /governance/admission` 会检查变更类型、必需 evidence、changed paths、质量面板和迁移状态
- `/contracts/versions` 已登记 `governance_policy` 与 `change_admission` 两个 P3 契约
- Portal 检视器已新增“治理准入”面板，可直接选择变更类型并检查 blockers / warnings / 推荐落点
- `tools/run_p3_governance_checks.ps1` 会先跑 P3 targeted non-live，再跑全量非 live

## P4: 自动化准入执行与 CI 门禁

目标：

- 把 P3 的准入结果升级成 CI、脚本和发布前流程可以直接消费的阻断信号
- 让本地 CLI、PowerShell、API 和 Portal 对“是否应该阻断”使用同一份 `governance_enforcement` 契约

当前落地：

- `/contracts/versions` 已登记 `governance_enforcement`
- `POST /governance/enforce` 会返回 `passed / should_block / exit_code / blocking_checks / admission`
- `python -m agent_system.cli governance` 支持 `--mode strict|advisory`、`--fail-on-warnings`、`--json`、`--evidence` 和 `--changed-path`
- `tools/enforce_governance.ps1` 提供 CI 友好的 PowerShell 包装，并直接透传 CLI 退出码
- `tools/run_p4_enforcement_checks.ps1` 会先跑 P4 targeted non-live，再跑全量非 live
- Portal 的“治理准入”面板已切到 `/governance/enforce`，会显示执行信息、exit code 和 should block 状态

## P5: 生产规模验证与真实项目交付

目标：

- 把前面建立的契约、目录、模板、质量、迁移和治理能力压成“真实项目交付前”的统一验证报告
- 让 vertical slice、内容生产链路和 release candidate 都可以被同一套 strict/advisory 结果检查

当前落地：

- `/contracts/versions` 已登记 `production_scenarios` 与 `production_readiness`
- `agent_system/tools/production_scale.py` 提供 `vertical_slice_2d / content_pipeline / release_candidate` 三个生产验证场景
- `GET /production/scenarios` 返回场景目录、required paths、required evidence、推荐 changed paths 和 gate focus
- `POST /production/validate` 会统一检查 required file tree、template registry、quality dashboard、migrations 和 governance admission
- `python -m agent_system.cli production` 与 `tools/validate_production_readiness.ps1` 提供本地/CI 可执行入口
- `tools/run_p5_production_checks.ps1` 默认跑 targeted 与全量 non-live；本地可追加 `-IncludeLive -IncludeBrowser -IncludeRemoteMcp` 执行真实 Godot sandbox、Portal browser smoke 和 Remote MCP smoke
- Portal 检视器已新增“生产验证”面板，可选择场景、填写 evidence/path 并查看 stage 结果
- 主项目已补齐 production 样本：`agent_templates/genres/platformer_production.json`、`telemetry/event_catalog.json`、`telemetry/sessions/vertical_slice_sample.jsonl` 和 `tests/baselines/performance/vertical_slice_sample_performance.json`
- `tests/test_production_samples.py` 会验证项目级模板覆盖、遥测样本、性能基线样本、质量面板和 P5 strict smoke 不再出现样本缺失 warning
- `tests/test_live_production_flows.py` 已补充扩展 live 验证，在 Godot 编辑器在线时覆盖美术导入、关卡模板、遥测回流、性能分析、数值平衡、P5 production validate 与 P6 agent compatibility
- Godot 实时桥接已新增 `POST /editor/operation` 与插件侧 `editor_operation` 命令，基础能力支持场景树读取、节点选择、节点创建、属性设置和节点删除；`tests/test_live_sandbox.py::test_6_typed_editor_operations` 会在真实编辑器里闭环验证这条链路

## P6: 多 Agent/API 兼容矩阵

目标：

- 让 Codex、Gemini、OpenAI API、Claude、local LLM 等不同接入方式共享同一套 contract、MCP tool schema、API surface、文件树和输出质量壳
- 避免不同 AI API 时代码质量、文件结构和测试入口漂移

当前落地：

- `/contracts/versions` 已登记 `agent_provider_compatibility`
- `agent_system/tools/agent_compatibility.py` 提供 provider profile 与兼容性矩阵
- `GET /agent-compat/providers` 返回 Codex、Gemini、OpenAI API、Claude、local LLM 的 profile 与 handoff contract
- `GET/POST /agent-compat/matrix` 会检查 contracts、skills、stdio MCP、remote MCP、API endpoint、governance、production readiness、file tree 和 output contract
- stdio/remote MCP tool schema 已新增 `godot_production_validate` 与 `godot_agent_compat`，外部 Agent 可以不绕 CLI 直接触发 P5/P6 检查
- `python -m agent_system.cli agent-compat` 与 `tools/run_p6_agent_compat_checks.ps1` 提供本地/CI 可执行入口
- Portal 检视器已新增“Agent 兼容性”面板，可按 provider 过滤并查看 surface 状态
- `tools/run_portal_browser_smoke.ps1` 已固化 Portal browser smoke，会启动本地 API 并用无头 Chrome/Edge 检查质量、治理、生产验证、Agent 兼容性和 MCP/IDE 面板是否可加载
- `tools/run_portal_browser_click_smoke.py` 已固化点击式 browser smoke，会真实触发治理、生产验证、Agent 兼容性和数据表预览
- `tools/run_remote_mcp_live_smoke.ps1` 已固化 Remote MCP live smoke，会启动 HTTP bridge 并调用 `godot_production_validate / godot_agent_compat`
- `tools/run_full_live_validation.ps1` 会串联 Godot live、Portal DOM/click 和 Remote MCP live 验证

## P7: Godot 引擎内实时操作扩展

目标：

- 把实时桥接从“基础节点操作”扩展到可支撑关卡与系统搭建的编辑器操作层
- 让保存、重载、节点结构调整、批量编辑、脚本挂载和场景实例化都走同一个 `editor_operation` contract
- 为后续 Portal、MCP、CLI 和外部 Agent 接入保留统一审计字段与回滚锚点

当前落地：

- `/editor/operation` schema 已升级到 `1.1`，新增 `audit.audit_id / rollback_anchor / requested_at` 等操作审计字段
- 插件侧已支持 `save_scene / save_scene_as / reload_scene`
- 插件侧已支持 `duplicate_node / reparent_node / rename_node / move_node_order`
- 插件侧已支持 `batch_set_properties / batch_create_nodes`
- 插件侧已支持 `attach_script / detach_script / instantiate_scene`
- `tests/test_api.py` 已覆盖 P7 操作排队、审计字段和必填字段校验
- `tests/test_live_sandbox.py::test_7_p7_editor_operations` 已在真实 Godot 编辑器中覆盖保存、重载、结构调整、批量操作、脚本挂载和场景实例化

## P8: 关卡编辑器级工作流闭环

目标：

- 把关卡制作从“单次模板生成”升级到“模板、验收、快照、差异回归”完整闭环
- 让关卡结构约束同时覆盖场景、manifest、API 和后续 Portal/MCP 接入

当前状态：

- `agent_system/skills/dev/level_workflow_skill.py` 已升级到 schema `1.1`
- 已支持 `navigation_zones / navigation_agents / tile_layers / trigger_zones / level_bounds`
- 已支持 `snapshot / diff`，输出结构化 `level_workflow_snapshot`
- `tests/test_level_workflow.py` 已覆盖模板、扩展审计、snapshot/diff 和 prompt 路由
- `tests/test_api.py` 已覆盖 `/levels/manage` 的模板、快照 diff 和审计失败返回
- `tests/test_live_production_flows.py::test_p8_level_workflow_manage_endpoint_supports_snapshot_and_diff_live` 已补充 live 验证
- `README.md` 与 `docs/TESTING_GUIDE.md` 已同步 P8 的 API 与验证入口

## P9: 按游戏类型补 Gameplay 系统模板

目标：

- 把项目模板从“目录和预算骨架”推进到“核心玩法系统骨架”
- 让不同 AI/API、Portal、CLI、MCP 都能读取同一份 starter gameplay systems contract
- 在蓝图中直接沉淀按游戏类型拆分的计划功能、依赖和验收项，减少后期补修

当前落地：

- `agent_system/tools/template_registry.py` 已新增 `starter_gameplay_systems` 校验和 `build_gameplay_template_snapshot(...)`
- 内建 `platformer / topdown_action / arpg / roguelike / tower_defense / visual_novel / survival_crafting` 现在都带 starter systems、依赖和 acceptance checks
- 项目级覆盖模板 `agent_templates/genres/platformer_production.json` 已同步到 P9 schema
- `agent_system/tools/blueprint_manager.py` 已支持 `gameplay_template_id / starter_gameplay_systems / upsert_gameplay_systems(...)`
- `init_game_blueprint` 现在会把 starter gameplay systems 和 seeded features 一并写入蓝图
- `agent_system/skills/architect/gameplay_template_skill.py` 已提供 `preview / apply` 两段式入口，并通过统一 `build_result(...)` 输出 skill result
- `agent_system/router.py`、`agent_system/skills/registry.py` 已支持“玩法模板 / 核心系统 / 玩法骨架”提示词路由
- API 已新增 `GET /gameplay/templates` 与 `POST /gameplay/manage`
- `tests/test_gameplay_template_skill.py` 已覆盖 P9 skill、蓝图落地和 prompt 路由
- `tests/test_api.py` 已覆盖 P9 API 快照、apply 和失败返回
- `tests/test_live_production_flows.py::test_p9_gameplay_template_manage_endpoint_supports_preview_and_apply_live` 已补充 API live 验证入口

## P10: 动画、VFX、Shader、Audio 专项链路

目标：

- 把表现层生产从零散脚本或自然语言描述，推进到统一 contract、统一文件树、统一 skill result 的受管管线
- 让动画、粒子、shader、音频事件都能通过同一份 manifest 和 scaffold 结构落地，减少后期命名漂移与调试成本
- 为 Portal、CLI、MCP 和外部 Agent 暴露稳定的 profile API，而不是继续依赖命令式拼接

当前落地：

- `agent_system/contracts/core.py` 与 `agent_system/contracts/catalog.py` 已新增 `presentation_profile` contract，schema version 为 `1.0`
- `agent_system/skills/resource/presentation_skill.py` 已提供 `manage_presentation_pipeline`，支持 `template / validate / preview / apply`
- 当前内建 `animation / vfx / shader / audio` 四类 profile，会统一生成 `assets/manifests/*_profiles.json`、`assets/shaders/`、`assets/materials/`、`assets/vfx/`、`scripts/presentation/` 与 `scripts/audio/` 下的受管产物
- `agent_system/router.py`、`agent_system/skills/registry.py`、`agent_system/roles/resource_manager.py` 已支持“表现层模板 / 动画树 / shader 模板 / 音频总线 / 音频事件 / 粒子模板”提示词路由
- API 已新增 `GET /presentation/profiles` 与 `POST /presentation/manage`
- `tests/test_presentation_pipeline_skill.py` 已覆盖 P10 skill、蓝图落地和 prompt 路由
- `tests/test_api.py` 已覆盖 P10 API 快照、preview/apply 和失败返回
- `tests/test_live_production_flows.py::test_p10_presentation_manage_endpoint_supports_preview_and_apply_live` 已补充 API live 验证入口

## P11: 美术 DCC 生产链路

目标：

- 把美术 intake 从“贴图/材质的简单拷贝”升级到可以承接 DCC 和外包交付的 profile 化约束
- 让 Blender/GLTF、Aseprite、Spine、Substance、外包 zip 都走同一份 manifest、同一份文件树和同一套 skill result
- 在导入前就阻断 sidecar 丢失、版本缺失、授权缺失、texture set 不全这类后期才会炸的问题

当前落地：

- `agent_system/skills/resource/art_asset_skill.py` 已升级到 schema `1.1`
- `manage_art_asset_pipeline` 现已新增 `model / aseprite / spine / substance / outsource` 五类 profile
- 新 profile 会校验 `allowed_source_extensions / dependency_paths / target_dependency_paths / source_tool / lod_count / texture_set / package_version / license_name`
- `agent_system/skills/registry.py`、`agent_system/router.py`、`agent_system/roles/resource_manager.py` 已支持 Blender/GLTF、Aseprite、Spine、Substance 和外包交付的提示词路由
- `agent_system/validations/project_layout.py` 已扩展 `assets/models / assets/characters/spine / assets/materials/substance / assets/packages/outsource` 等受管目录和对应扩展名
- API 已新增 `GET /art-assets/profiles` 与 `POST /art-assets/manage`
- `tests/test_art_pipeline.py` 已覆盖 P11 skill、sidecar copy、外包 metadata 校验和 prompt 路由
- `tests/test_api.py` 已覆盖 P11 art intake API 快照、preview/apply 和失败返回
- `tests/test_live_production_flows.py::test_p11_art_dcc_profiles_execute_live` 与 `tests/test_live_production_flows.py::test_p11_art_asset_manage_endpoint_supports_model_profile_live` 已补充 `/execute` 与 `/art-assets/manage` 两条 live 验证入口

## P12: 运营趋势与 LiveOps 联动闭环

目标：

- 把 telemetry 从“单次分析”升级到“趋势 + 配置影响”双视角 dashboard
- 让 retention/funnel 趋势和 LiveOps manifest 联动成为稳定 contract，而不是临时报告

当前落地：

- `telemetry_summary` 已扩展 `retention_funnel_trend_dashboard / liveops_impact_dashboard`
- API 已新增 `GET /telemetry/trends` 与 `GET /liveops/impact-dashboard`
- Portal 已支持 `导出趋势报告`、`影响报告`
- 仓库已补齐 `liveops/remote_config.json` 与 `liveops/experiments.json` 项目级样本
- `tests/test_telemetry_pipeline.py`、`tests/test_api.py`、`tests/test_quality_dashboard.py` 与 `tests/test_production_samples.py` 已补齐趋势与 LiveOps impact 回归

## P13: 平台交付 / Savegame 基线

目标：

- 把平台发布、savegame schema、服务开关和多人配置纳入单一 baseline
- 避免平台参数、云存档、多人 transport 在发布前才分散暴露问题

当前落地：

- `platform_delivery_profile` contract 已登记到 `/contracts/versions`
- `manage_platform_delivery` 已支持 `template / validate / preview / apply`
- API 已新增 `GET /platform-delivery/profile` 与 `POST /platform-delivery/manage`
- Portal 已新增 `平台交付 / Savegame` 面板
- 仓库已补齐 `deployment/platform_delivery.json` 项目级样本
- `tests/test_platform_delivery_skill.py`、`tests/test_api.py` 与 `tests/test_project_layout.py` 已补齐基础回归

## P14: 重型性能画像基线

目标：

- 把性能分析从简单 budget pass/fail 提升到带 `frame_breakdown / memory_trend` 的画像层
- 让质量面板、发布门禁和 Portal 导出共享同一份 richer profile

当前落地：

- `performance_summary` 已升级到 `1.1`
- `agent_system/tools/performance_analysis.py` 已支持 `frame_breakdown / memory_trend`
- `GET /performance/dashboard` 与 Portal `导出画像报告` 已接通 richer profile 导出
- 质量面板会汇总 `top_frame_stage / max_memory_growth_mb`
- 仓库性能样本 `tests/baselines/performance/vertical_slice_sample_performance.json` 已补齐 `frame_breakdown / memory_trend`
- `tests/test_performance_pipeline.py`、`tests/test_api.py`、`tests/test_quality_dashboard.py` 与 `tests/test_production_samples.py` 已补齐回归

## P15: Release Candidate Checklist

目标：

- 把当前包是否具备 RC 交付条件收敛成统一 contract，而不是继续分散在 release manifest、QA gate、production gate 和 Portal 卡片里
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `should_block / warning_count / blocked_count / recommendations`

当前落地：

- `agent_system/tools/release_candidate.py` 已提供统一 `build_release_candidate_checklist(...)`
- `/contracts/versions` 已登记 `release_candidate_checklist`
- API 已新增 `GET /release-candidate/checklist` 与 `POST /release-candidate/checklist`
- Portal 已新增 `Release Candidate` 面板，可直接填写 manifest/evidence/path 并查看 `release_manifest / quality_gate / performance_gate / telemetry_gate / rollback / production_gate`
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把 RC 面板纳入浏览器 smoke
- `tests/test_release_candidate.py`、`tests/test_contracts.py` 与 `tests/test_api.py` 已补齐 RC contract、API 和 Portal 回归

## P15: Outsource Delivery Gate

目标：

- 把外包交付包从“只是 art intake 的一种类型”提升为独立可执行 gate，避免后期发布前再靠人工补 license、package root 和 traceability
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `delivery_count / blocking_checks / warning_checks / should_block`

当前落地：

- `agent_system/tools/outsource_delivery.py` 已提供统一 `build_outsource_delivery_gate(...)`
- `/contracts/versions` 已登记 `outsource_delivery_gate`
- API 已新增 `GET /outsource-delivery/gate` 与 `POST /outsource-delivery/gate`
- Portal 已新增 `外包交付 Gate` 面板，可直接填写 manifest/package root/license 白名单并执行 Gate
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把外包交付 Gate 纳入浏览器 smoke
- `tests/test_outsource_delivery.py`、`tests/test_contracts.py` 与 `tests/test_api.py` 已补齐 gate contract、API 和 Portal 回归

## P15: Asset Review Workflow

目标：

- 把资产评审状态从“口头约定”落成可持久化 review board，避免 art intake 落盘后审批结果散落在聊天记录或 release note 里
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `pending_review / approved / returned / reviewer / review_note`

当前落地：

- `agent_system/tools/asset_review.py` 已提供统一 `build_asset_review_workflow(...)` 与 `apply_asset_review_decision(...)`
- `/contracts/versions` 已登记 `asset_review_workflow`
- API 已新增 `GET /asset-reviews/workflow` 与 `POST /asset-reviews/manage`
- Portal 已新增 `资产评审` 面板，可按资产类型读取评审状态、批量通过/退回或重置待评审
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把资产评审纳入浏览器 smoke
- `tests/test_asset_review.py`、`tests/test_contracts.py` 与 `tests/test_api.py` 已补齐 workflow contract、API 和 Portal 回归

## P15: Build / Run Matrix

目标：

- 把“平台交付 target、production gate、RC checklist、本地非 live/browser/live/remote MCP 验收 lane”合并成一份统一 matrix，避免发布前靠 README 手工拼命令
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `rows / blocking_rows / warning_rows / default_sequence / should_block`

当前落地：

- `agent_system/tools/build_run_matrix.py` 已提供统一 `build_build_run_matrix(...)`
- `/contracts/versions` 已登记 `build_run_matrix`
- API 已新增 `GET /build-run/matrix` 与 `POST /build-run/matrix`
- Portal 已新增 `Build / Run Matrix` 面板，可直接查看 build target、production gate、RC checklist 和推荐执行顺序
- Matrix 现在包含 `runtime_performance_sampling` 默认 lane，用于把最近一次运行时性能采样作为发布前矩阵证据；缺少 profile 时给 warning，有 profile 且预算失败时给 blocked
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把 Build / Run Matrix 纳入浏览器 smoke
- `tests/test_build_run_matrix.py`、`tests/test_contracts.py` 与 `tests/test_api.py` 已补齐 matrix contract、API 和 Portal 回归

## P15: Scene Ownership / Lock Hints

目标：

- 把“场景归属、功能责任边界、多人协作锁定状态”从口头约定落成可持久化 board，避免多人同时改同一关卡或模块场景
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `owner / feature_id / lock_state / orphan_count / should_block`

当前落地：

- `agent_system/tools/scene_ownership.py` 已提供统一 `build_scene_ownership_board(...)` 与 `apply_scene_ownership_update(...)`
- `/contracts/versions` 已登记 `scene_ownership_board`
- API 已新增 `GET /scene-ownership/board` 与 `POST /scene-ownership/manage`
- 场景协作 board 固定落到 `scenes/scene_ownership_board.json`，并会扫描 `scenes/**/*.tscn` 与 `agent_modules/scenes/**/*.tscn`
- Portal 已新增 `场景归属 / 锁定` 面板，可直接认领锁定、设为共享或释放场景
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把 scene ownership 纳入浏览器 smoke
- `tests/test_scene_ownership.py`、`tests/test_contracts.py`、`tests/test_project_layout.py` 与 `tests/test_api.py` 已补齐 board builder、contract、layout 和 Portal 回归

## P16: Release Promotion Plan

目标：

- 把“RC checklist、build/run matrix、Agent 兼容性、场景协作状态、signoff、promotion evidence、deployment rehearsal、rollback rehearsal”合并成一次渠道晋级决策，而不是继续靠人工在多个面板之间切换判断
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `promotion_steps / missing_signoffs / selected_scenario_ids / selected_provider_ids / should_block / evidence_bundle / deployment_rehearsal / rollback_rehearsal`

当前落地：

- `agent_system/tools/release_promotion.py` 已提供统一 `build_release_promotion_plan(...)`
- `agent_system/tools/release_promotion.py` 现已同时提供 `build_release_promotion_evidence_report(...)`、`build_deployment_rehearsal_report(...)` 与 `build_rollback_rehearsal_report(...)`
- `/contracts/versions` 已登记 `release_promotion_plan`
- API 已新增 `GET /release-promotion/plan`、`POST /release-promotion/plan`、`GET /release-promotion/evidence-report`、`GET /release-promotion/deployment-rehearsal` 与 `GET /release-promotion/rollback-rehearsal`
- P16 现已完整聚合 `release_candidate_checklist / build_run_matrix / agent_provider_compatibility / scene_ownership_board / signoff_gate / evidence_bundle / deployment_rehearsal / rollback_rehearsal`
- Portal 已新增 `Release Promotion` 面板，可直接查看 target channel/environment、required signoffs、promotion steps、evidence/rehearsal 摘要，并导出三类 Markdown runbook 到源码预览区
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把 Release Promotion 纳入浏览器 smoke
- `tests/test_release_promotion.py`、`tests/test_contracts.py` 与 `tests/test_api.py` 已补齐 contract、API、导出报告和 Portal 回归

## P17: Release Promotion History

目标：

- 把真实的渠道晋级结论、执行人、signoff 来源和 plan 快照持久化，避免 release promotion 只存在于聊天、Portal 当前状态或临时 Markdown 导出中
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `decision / executed_by / target_channel / release_build_id / plan_snapshot / evidence_status / deployment_status / rollback_status`

当前落地：

- `agent_system/tools/release_promotion_history.py` 已提供统一 `build_release_promotion_history(...)` 与 `record_release_promotion_event(...)`
- `/contracts/versions` 已登记 `release_promotion_history`
- API 已新增 `GET /release-promotion/history` 与 `POST /release-promotion/record`
- promotion history manifest 固定落到 `deployment/release_promotion_history.json`，并支持 `decision / target_channel / executed_by` 过滤与分页
- Portal 的 `Release Promotion` 面板现在可直接读取 history，并记录实际 `approved / promoted / blocked / rolled_back / aborted` 结果
- `tools/run_portal_browser_click_smoke.py` 已把 release promotion history record 纳入浏览器 click smoke
- `tests/test_release_promotion.py`、`tests/test_contracts.py` 与 `tests/test_api.py` 已补齐 builder、contract、API 和 Portal 回归

## P18: Release Execution / Rollout Control

目标：

- 把 `release promotion plan + history` 进一步落到真实的执行账本，避免晋级只停留在审批结论，缺少 canary / full rollout / rollback 的状态闭环
- 让 Portal、API、MCP 和外部 Agent 都能读取同一份 `dry_run / canary / full_rollout / rollback` execution ledger，以及当前 channel binding、active URL、versioned URL、rollback 锚点

当前落地：

- `agent_system/tools/release_execution.py` 已提供统一 `build_release_execution_status(...)`、`run_release_execution(...)` 与 `rollback_release_execution(...)`
- `/contracts/versions` 已登记 `release_execution_status`
- API 已新增 `GET /release-execution/status`、`POST /release-execution/run` 与 `POST /release-execution/rollback`
- execution ledger 与 channel manifest 固定落到 `deployment/release_execution_status.json` 与 `deployment/release_channels.json`
- `dry_run` 只做 rehearsal，不修改 channel binding；`canary / full_rollout` 会更新 active channel URL；`rollback` 会恢复到 rollback target 并记录恢复状态
- Portal 已新增 `Release Execution` 面板，可直接执行 `dry_run / canary / full_rollout / rollback` 并查看 checklist、active channel binding 与最近 execution
- `tools/run_portal_browser_smoke.ps1` 与 `tools/run_portal_browser_click_smoke.py` 已把 Release Execution 纳入浏览器 smoke，其中 click smoke 会覆盖 `dry_run -> canary -> full_rollout -> rollback`
- `tests/test_release_execution.py`、`tests/test_contracts.py`、`tests/test_api.py`、`tests/test_project_layout.py` 与 `tests/test_agent_compatibility.py` 已补齐 builder、contract、API、目录规范与 Portal 回归

## P19: Zero-to-Playable Game Creation Wizard

目标：

- 把“从零创建一个可玩 2D 原型”的入口收敛成统一计划、脚手架和 MCP/API contract，而不是只靠自然语言逐步生成散落文件
- 让客户试用、MCP 调用和本地 Web/Portal 工作流能先拿到结构化计划，再选择是否写入 Godot 项目
- 让生成物默认落到受管 `scenes/`、`scripts/`、`data_tables/` 目录，减少首次试用时的文件树漂移

当前落地：

- `agent_system/tools/game_creation_wizard.py` 已提供 `list / plan / apply` 能力，当前模板覆盖 `platformer_2d / topdown_action_2d / tower_defense_2d / arpg_2d / roguelike_2d / visual_novel_2d / survival_crafting_2d`
- `/contracts/versions` 已登记 `game_creation_profile`、`game_creation_replay` 与 `game_creation_template_migration`
- API 已新增 `GET /game-creation/templates`、`POST /game-creation/plan` 与 `POST /game-creation/apply`
- API 已新增 `POST /game-creation/audit-scene-graph`，用于把 `game_creation_profile` 与生成后的 `.tscn/.gd` 做计划对照审计
- API 已新增 `POST /game-creation/review`，用于汇总验收清单、模块状态、场景树审计摘要和阻断项
- API 已新增 `POST /plugin/scene-snapshot` 与 `GET /plugin/scene-snapshot`，Godot 插件会随 editor state 回传实时 `scene_graph_snapshot`
- Portal 已新增“游戏创建向导”面板，可读取模板、预览计划、写入脚手架、覆盖写入、审计场景树或生成验收摘要
- CLI 已新增 `python -m agent_system.cli game-create`，支持 `--templates / --apply / --overwrite / --audit / --review / --write-report / --json`
- stdio/remote MCP tool schema 已新增 `godot_create_game_plan`、`godot_apply_game_plan`、`godot_audit_game_scene_graph` 与 `godot_review_game_creation`
- 脚手架会生成 `project.godot`、`scenes/Main.tscn`、`scenes/Player.tscn`、`scenes/Coin.tscn`、`scenes/Enemy.tscn`、Main/玩家/金币/敌人/HUD/生命脚本，以及 `data_tables/game_creation/gameplay.json` 与 `data_tables/game_creation/input_replay.json`
- Main 场景已包含地面碰撞、Camera2D、HUD Label、金币计分连接和敌人伤害连接，作为非 live 可检查的最小可玩结构
- `topdown_action_2d` 会生成 ArenaBounds、四向移动、attack 输入、Pickup 收集、Enemy chase、HUD/health 连接和对应 `gameplay.json`
- `tower_defense_2d` 会生成 Placement Cursor、Tower、Enemy wave、Base health、resources HUD、`place_tower` 输入和对应 `gameplay.json`
- `arpg_2d` 会生成 QuestArena、Hero movement、attack/dodge 输入、Relic quest pickup、Enemy chase、Quest/Action HUD 和对应 `gameplay.json`
- `roguelike_2d` 会生成 DungeonRoom、Stairs、Hero movement、attack/descend 输入、Loot pickup、Enemy chase、Depth/Loot/Action HUD 和对应 `gameplay.json`
- `visual_novel_2d` 会生成 Stage、Reader controller、Choice、Npc portrait、Dialogue/Affinity HUD、advance/select 输入和对应 `gameplay.json`
- `survival_crafting_2d` 会生成 Field、Resource、Campfire、Survivor movement、gather/craft 输入、Wood/Hunger/Health HUD 和对应 `gameplay.json`
- `game_creation_profile` 现在包含 `input_replay_plan / golden_screenshot_plan`，每个模板都会沉淀打开场景、执行输入、捕获截图、baseline 路径、允许 diff 阈值和必需节点约束；`game_creation_review` 会分别校验 `gameplay_balance` 与 `game_creation_input_replay` 数据表
- `game_creation_replay` contract 已落地，`POST /game-creation/replay` 与 `game-create --replay --write-report` 会读取 `data_tables/game_creation/input_replay.json`，校验 `project.godot` 输入动作、Main 场景和 golden screenshot 必需节点，并生成 `logs/test_artifacts/game_creation/input_replay_<template>.gd` headless 回放脚本与 `data_tables/game_creation/input_replay_run.json`；传入 `execute_replay=true` 或 CLI `--execute-replay` 时会调用 Godot 执行脚本，默认 `replay_render_mode=headless`，也可通过 CLI `--replay-render-mode viewport` 或 API `replay_render_mode=viewport` 请求非 headless `--script` 运行；传入 `promote_baseline=true` 或 CLI `--promote-baseline` 时会把 runtime screenshot 显式登记到 `tests/baselines/screenshots/game_creation/`，并记录 baseline source/path/promoted_at；报告还会标准化 `replay_render_mode / screenshot_capture_mode / viewport_baseline_status / viewport_baseline_ready / viewport_baseline_message`，用于区分 headless fallback 截图证据和真正 viewport golden baseline 是否就绪
- `game_creation_template_migration` contract 已落地，`POST /game-creation/template-migration`、CLI `game-create --template-migration --to-template-id arpg` 与 MCP `godot_plan_game_template_migration` 会从 manifest 读取源模板，生成目标模板兼容检查、受管文件操作计划、数据表迁移策略、验证计划和 rollback 计划；默认只规划并可写出 `data_tables/game_creation/template_migration_plan.json`，不会直接改写项目文件
- `game_creation_profile` 现在包含 `module_plan / skill_binding_plan / block_diagram / godot_response_map`，块状结构图会按模块依赖生成；每个模块会绑定项目 Skills、职责角色、约束条件和 Godot 响应，Portal 面板会直接展示模块脚本、Skills、约束、块状结构图和 Godot 触发响应映射
- `game-create --apply` 写入前已对 `scripts/ / scenes/ / data_tables/` 受管输出执行 `ProjectLayoutValidator`，并把 `layout_checks / layout_passed / layout_blocking_checks` 写入 `game_creation_profile`，非法受管路径会在落盘前阻断
- `game-create --apply` 写入前已接入 `game_creation` 治理准入强校验，要求 `contract / layout / schema / preview_or_diff / quality_gate / rollback / tests / docs` evidence，并把 `governance_enforcement / governance_admission / governance_passed / governance_blocking_checks` 写入 `game_creation_profile`
- `scene_graph_audit` contract 已落地，会检查 manifest、场景存在性、节点类型/节点名、脚本挂载、信号声明/连接、Godot 响应目标，并可写出 `data_tables/game_creation/scene_graph_audit.json`
- `game_creation_review` contract 已落地，会把 acceptance criteria、模块 review、数据表 review、scene graph audit 摘要、blocking/warning checks 和 `ready_for_acceptance` 统一成验收结果，并可写出 `data_tables/game_creation/game_creation_review.json` 与面向 QA/策划阅读的 `docs/game_creation_review.md`
- `scene_graph_snapshot` contract 已落地，实时快照会记录当前 scene、root node、节点路径/类型/脚本/实例来源，并优先用于 `audit-scene-graph` 与 `review` 的监控判断
- `scene_graph_audit` 与 `game_creation_review.audit_summary` 已显式记录 `live_snapshot_used / live_snapshot_source / live_snapshot_scene_path / live_snapshot_node_count`，Portal 也会显示 live 监控证据，避免静态 `.tscn` 审计和真实 Godot editor 快照混在一起
- `game_creation_profile` manifest 固定落到 `data_tables/game_creation/game_creation_profile.json`
- P0 layout policy 已继续覆盖 legacy code 生成与修改入口：`generate_movement_script`、`generate_ai_behavior`、`generate_dialogue_system`、`wire_signal_connection`、`apply_tween_animation`、`manage_signal_bus` 在写入或修改 `.gd` 前会调用 `ProjectLayoutValidator`，阻断空格命名、路径逃逸、错误目录或非 `.gd` 输出，并把 `layout_check` 保留在 `skill_result.validation` 里
- 编辑器操作类 legacy skill 也已接入同一策略：`manage_input_mapping` 校验 `project.godot`，`attach_script_to_node` 校验待挂载脚本和离线目标场景，`create_godot_scene` 与 `setup_3d_environment` 在派发 editor/headless 创建脚本前校验目标 `.tscn` 路径
- 边缘编辑器操作链路 `instantiate_scene_prefab` 与 `configure_physics_collision` 也已接入 `ProjectLayoutValidator`，分别在实例化预制件和 headless 物理烘焙前校验目标 `.tscn` 路径
- `ProjectLayoutValidator` 已补齐 `repair_preview` 自动修复预览；非法路径会返回建议落点、问题代码和目录/文件名调整清单，且 `apply_mode=preview_only`，不会自动移动或写入文件
- Skill 统一基线已继续收口：skill 层不再直接裸返回 `ToolResult`，统一经 `BaseSkill.build_result()` 输出 `skill_result` envelope；`manage_audio_resource` 与独立 `audit_godot_resources` 旧入口已补齐 validation、rollback、artifact metadata 和资源审计报告 layout 证据
- `tests/test_live_sandbox.py::test_8_p19_scene_graph_snapshot_reaches_health_monitor` 已加入 live sandbox，用于验证 Godot 插件真实回传的场景树快照能进入 API `/health` 监控面
- `tests/test_live_sandbox.py::test_9_p19_runtime_playability_smoke_generated_tower_defense` 已加入 live sandbox，会把 `tower_defense_2d` 生成到真实 Godot 项目、打开 `res://scenes/Main.tscn`、等待 live scene snapshot、触发当前场景截图运行检查，并用 live snapshot 生成 `game_creation_review`
- `tests/test_live_sandbox.py::test_10_p19_execute_replay_generates_runtime_screenshot` 已加入 live sandbox 固定 lane，会调用 `/game-creation/replay` 的 `execute_replay=true / promote_baseline=true`，验证 headless replay 实际执行、`input_replay_run.json` 报告、runtime screenshot 产物和 golden baseline promotion
- 2026-05-01 已用真实 Godot 4.5.1 + Vulkan 对 `sandbox_project` 执行 `game-create --replay --execute-replay --replay-render-mode viewport --promote-baseline --write-report`，stdout 返回 `REPLAY_SCREENSHOT_CAPTURE=viewport`，报告记录 `viewport_baseline_ready=true`，并将 `logs/test_artifacts/game_creation/tower_defense_runtime.png` 推广到 `tests/baselines/screenshots/game_creation/tower_defense_2d_main.png`
- `tools/run_full_live_validation.ps1` 的 `godot_live_sandbox` lane 已把 P19 execute-replay 子链写入结构化 lane report：`flow_statuses.p19_execute_replay_flow`、`expected_live_tests`、`p19_replay_evidence` 和 replay report/script/runtime screenshot artifact 路径会随 `full_live_validation` 证据进入发布复核链
- `tests/test_game_creation_wizard.py`、`tests/test_mcp_server.py`、`tests/test_remote_mcp_bridge.py`、`tests/test_contracts.py`、`tests/test_cli.py` 与 `tests/test_api.py` 已补齐基础回归

## 推荐落地顺序

当前路线图状态已可机器读取：

- `GET /roadmap/status`
- `python -m agent_system.cli roadmap --json`

该输出会按 `roadmap_status` contract 汇总 `done / partial / pending / remaining_items / next_recommended_actions`，用于判断下一步优先推进哪一块。

## 2026-05-01 项目完成收口状态

- 路线图机器状态：`30/30 done`，`partial=0`，`pending=0`，`remaining=0`
- 真实 viewport replay：已用 Godot 4.5.1 + Vulkan 执行 `game-create --project-root sandbox_project --replay --execute-replay --replay-render-mode viewport --promote-baseline --write-report --json`，报告返回 `REPLAY_SCREENSHOT_CAPTURE=viewport`、`viewport_baseline_ready=true`、`baseline_promoted=true`
- P19 golden baseline：`sandbox_project/logs/test_artifacts/game_creation/tower_defense_runtime.png` 已推广到 `sandbox_project/tests/baselines/screenshots/game_creation/tower_defense_2d_main.png`
- 非 live 回归入口：新增 `tools/run_non_live_validation_shards.ps1`，用于替代单条 `pytest -m "not live"` 的超长串行运行；脚本会写出 `logs/reports/non_live_validation_shards.json` 与 `logs/reports/non_live_validation_shards.md`
- 推荐命令：`.\tools\run_non_live_validation_shards.ps1 -PythonCommand D:\actions-tools\Python312\python.exe -ContinueOnFailure`
- 已验证 shard：`telemetry_templates` 通过 `37 passed`，`promotion_redacted` 通过 `1 passed`，并验证了 shard timeout 路径会返回 `status=timeout / exit_code=124`
- 已知慢测试族：`test_release_promotion.py`、`test_release_execution.py`、`test_release_ci_artifacts.py` 和 `test_build_run_matrix.py` 会构建完整 release / promotion / matrix 证据，单文件或单用例可能耗时数分钟；后续应优先使用 shard 脚本分批执行

1. 先补 `统一契约层`
2. 再补 `文件树与命名规范引擎`
3. 然后做 `模板注册中心`
4. 再统一 `Skill 基线`
5. 接着补 `预算系统与回放资产`
6. 最后按模块推进美术、关卡、数值、遥测、性能
7. 进入 P3 后，所有新增变更先通过 `governance/admission` 声明 evidence 和 changed paths
8. 进入 P4 后，CI 和发布前检查统一调用 `governance/enforce` 或 `tools/enforce_governance.ps1`
9. 进入 P5 后，真实项目交付前统一调用 `production/validate` 或 `tools/validate_production_readiness.ps1`
10. 进入 P6 后，新增 Agent/API 接入先通过 `agent-compat/matrix` 校验 provider surface 和 handoff contract

## 建议目录骨架

```text
agent_system/
  contracts/
  skills/
    art/
    level/
    balance/
    telemetry/
    performance/
  templates/
    genres/
  validations/
docs/
  标准化规范/
migrations/
tests/
  baselines/
    screenshots/
    performance/
    telemetry/
```

## 进入实施前还应补的一项

建议再增加一条：

- `项目治理与变更准入规则`

原因：

- 没有准入规则，标准化文档很容易沦为“建议”
- 需要明确哪些增强必须带 schema、测试、模板、目录策略和迁移说明

最低要求可以是：

- 新 skill 必须带输入输出契约
- 新目录必须登记用途和清理策略
- 新模板必须带版本号
- 新遥测事件必须登记字段与隐私说明
- 新性能指标必须登记预算来源

当前状态：

- 已纳入 P3 并落地为机器可读治理策略，不再只停留在文档建议。
