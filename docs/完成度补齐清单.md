# 完成度补齐清单

按 2026-04-08 当前仓库状态整理。

## 当前基线

- 自动化测试结果: `86 passed`
- 已通过区域: Router、CLI、API、资源审计/修复、安全重构、基础集成、真实 live sandbox 联动
- 已补齐基础沙箱工程: `sandbox_project/`
- 已闭环区域: 真实 `api_server + Godot 编辑器` 联动验收
- 当前主要风险: 不是 Python 逻辑，而是运行产物治理、CI 扩面和发布说明完善

## P0 必须完成

### 1. 提供可重复的 `sandbox_project`

状态: 已完成基础版本

目标:

- 仓库内有一个可直接启动的最小 Godot 沙箱工程
- 能覆盖 `open-resource / inject-node / create-scene / screenshot / websocket` 这几类 live 用例

当前结果:

- 已新增 `sandbox_project/`
- 已包含 `project.godot`、`sandbox_main.tscn`、`scripts/sandbox_root.gd`
- 已复制 `sandbox_project/addons/godot_agent/` 插件目录

验证结果:

- 已通过 `tools/start_live_sandbox.ps1 -ApiPort 8011`
- `sandbox_project/` 能稳定连上 API Server 并通过 live 测试

### 2. 固化真实联调启动流程（基础脚本已补齐）

现状:

- 已新增 `tools/start_live_sandbox.ps1`
- 已新增 `tools/stop_live_sandbox.ps1`
- 已新增 `tools/run_live_sandbox_tests.ps1`
- `start_live_sandbox.ps1` 会同步 `sandbox_project/addons/godot_agent/`、启动 API、再调用 `/editor/launch` 等待编辑器上线

验证结果:

- 已在真实环境下完成冷启动
- 已跑通 `python -m pytest tests/test_live_sandbox.py -v -s`
- 现有 `run_live_sandbox_tests.ps1` 已将启动、测试和清理固化成单命令入口

已达成:

- 启停脚本入口已经固定
- 端口可通过同一套环境变量控制
- 不需要手动复制插件或手动点开场景

### 3. 统一端口配置（基础版已完成）

现状:

- 代码已支持通过 `GODOT_AGENT_API_HOST` / `GODOT_AGENT_API_PORT` 统一配置
- `api_server/main.py`、`start.py`、插件、`tests/test_live_sandbox.py` 已跟随同一配置
- 当前机器上 `0.0.0.0:8000` 出现 `WinError 10013`，本地开发更适合显式切到 `8011`

验证结果:

- 已用 `8011` 端口完成真实 `api_server + Godot 编辑器` 联调
- 现有文档和脚本已能支撑非默认端口启动

已达成:

- 支持类似 `GODOT_AGENT_API_PORT=8011`
- 端口变更后无需手改多处文件
- 文档示例已改为统一环境变量方案

### 4. 跑通 `tests/test_live_sandbox.py`

目标:

- 不再依赖“机器上恰好已经有一个在线编辑器”
- 在标准启动流程下通过所有 live 用例

完成标准:

- `python -m pytest tests/test_live_sandbox.py -v -s` 全通过
- 包含:
  - 资源打开
  - 节点注入
  - 场景创建
  - 截图产物
  - Portal WebSocket 实时事件

## P1 应该完成

### 已完成结论

- `tests/test_live_sandbox.py` 已全通过
- 真实联动覆盖资源打开、节点注入、场景创建、截图产物、Portal WebSocket 实时事件
- 当前不再需要把 live sandbox 视为 P0 未闭环项

### 5. 清理运行产物与仓库边界

目标:

- 运行时文件和源码边界更清晰

建议处理:

- 评估 `logs/` 是否需要只保留必要样例
- 评估 `.godot/`、测试临时目录、历史产物是否应进一步忽略
- 避免让仓库状态长期被运行副产物污染

完成标准:

- 开发者首次 clone 后能分清“源码、示例、缓存、产物”

当前结果:

- `.gitignore` 已补齐 `.godot/`、`logs/`、`tests/.tmp*`、`pytest-cache-files-*`、`tests/test_report.json` 以及 `api_server/static/dist/` 下生成发布产物的忽略规则
- 已新增 `tools/clean_runtime_artifacts.ps1`
- 已新增 `tools/fix_acl_artifacts.ps1`
- 可先通过 `.\tools\clean_runtime_artifacts.ps1 -Preview` 预览清理范围，再决定是否执行实际清理；当前也会覆盖 `api_server/static/dist/` 下的历史 `web_*` 版本目录和 stable release 副产物
- 清理脚本已补上结果汇总；如命中 ACL 拒绝访问的历史残留，会直接输出管理员 PowerShell 修复命令
- `fix_acl_artifacts.ps1` 默认只匹配需要 ACL 接管的托管运行产物，可先用 `-Preview` 复核范围

### 6. 建立非 live CI

目标:

- 每次改动自动跑稳定测试

建议命令:

```bash
python -m pytest -m "not live"
```

完成标准:

- 至少有一套 CI 运行 `tests/test_agent.py`、`tests/test_api.py`、`tests/test_integration.py`

当前结果:

- `.github/workflows/non-live-tests.yml` 已扩成两层
- 第一层跑稳定子集，包含 `tests/test_godot_cli.py`
- 第二层跑 `python -m pytest -m "not live" -q` 的全量非 live 回归

### 7. 同步插件双份目录策略

现状:

- 仓库里同时存在 `addons/godot_agent/` 和 `godot_plugin/addons/godot_agent/`

目标:

- 明确哪一份是运行态，哪一份是分发源
- 避免两份插件内容漂移

完成标准:

- 文档说明同步策略
- 最好提供同步脚本或单一来源

当前结果:

- 已将 `addons/godot_agent/` 定义为插件唯一源码
- 已新增 `tools/sync_plugin.ps1`
- `godot_plugin/addons/godot_agent/` 作为分发副本
- `sandbox_project/addons/godot_agent/` 作为 live sandbox 测试副本
- `doctor` 会检查运行态与分发副本是否漂移，并提示运行同步脚本

## P2 可选优化

### 8. 增加真实演示资产

目标:

- 让外部使用者更快理解效果

建议内容:

- 一个可运行 demo 场景
- 一组固定演示指令
- 一份截图或录屏

### 9. 增加发布说明

目标:

- 让插件和 Python 服务可以独立交付

建议内容:

- 版本兼容矩阵
- 安装步骤
- 已知限制

## 推荐执行顺序

1. 先清理运行产物与仓库边界
2. 再补非 live CI
3. 然后明确插件双份目录同步策略
4. 最后再补演示资产和发布说明

## 下一轮建议直接做的事

如果继续往前推，优先做这两个：

1. 清理 `logs/`、`.godot/` 和测试临时产物的仓库边界
2. 视需要再把 CI 从稳定子集扩到更多非 live 用例

补充:

- 如果下一步要从“技术可用”转向“制作可用”，请直接参考 `docs/制作路线图.md`
